# useContext
上下文? 
## 组件通信
- 父子组件通信
  - 单项数据流
  - props 传递数据 自定义事件
  共享数据， 统一数据状态管理
- 兄弟关系
- 爷孙关系
原来的那套 层层传递 层次太深， 搬运工作就太复杂了！
- 陌生人关系

## **组件层次**比较深
  - useContext 上下文
  - 提供数据上下文  最外层
  Context.Provider 组件
  - 消费上下文
  跨越层级， 不考虑层级
  连接上下文， 消费数据

  ## useContext 消费上下文
  useContext 可以跨层级直接读取组件上下文数据，省去逐层向下传递 props。

  ## 步骤
  1. createContext 创建上下文
  2. Provider 组件包裹组件树
  3. useContext 消费上下文

  ## 自定义hooks 
  - use 开头的函数
  - hooks 目录下， 属于架构的一部分
  - 封装了响应式、副作用等功能， 复用

  ## 监听鼠标移动事件， 鼠标坐标显示在页面上
  - 抽象
    响应式的鼠标坐标 
  - 封装
    hooks 目录下