# 栈和队列
基于对数组的理解和掌握，线性数据结构(栈、队列和链表)
## 非线性数据结构，
树& 图
## 线性数据结构
- 数组和链表
数组 开箱即用 连续存储+ 下标访问
链表 不连续 
- 栈和队列 


## 栈和队列 特性
看成操作受限的数组， 特殊的数组
栈只能在栈顶操作，数组的尾部操作。后进先出
队列只能在队尾入队，在对头出队shift()先进先出
也可以用链表来表示 , 

## 灵活增删的数组
- 增加元素的方法
 push  unshift  splice
 push 数组扩容的问题 链表没有这个问题
 unshift 内存视角 
 - splice 删 增(slice+ replace)
 MDN 上看 
(splice(start, deleteCount, item1, item2, /* …, */ itemN))
- 数组的增删 都不是纯函数， 找原数组

## 栈 stack
LIFO Last In First Out数据结构， **特殊** 的数组

冰柜里的雪糕
push/pop 受限的操作 peek length-1
while(stack.length)

## 队列 queue
- 受限 push / shift

## 链表
- 数组增加元素，arr.length >=
capacity 扩容和初始化 内存申请分配
的开销， 其余时刻没有的
- 链表 每次都申请 平均的
数据的规模
小 数组 
大规模  链表
数组的优势 遍历， 索引取元素 ，缺点是增删， 移动位置和申请空间
链表的优势 增删  (前驱节点，不要坐过站) 缺点是找到相应的节点


链表和数组相似的， 他们都是有序的列表(list)
array 索引
linked list head-> node....   -> tail

链表中， 数据单位节点 ，指针
节点 和节点的分布是不连续的，是离散的
// js 用对象字面量
{
    val:1,
    next:{
        val:2,
        next: 

    }
}
head ? 节点 
访问链表中的任何一个元素， 从起始节点开始(val 空)， 逐个访问
next ，一直访问到目标节点为止，tail 节点
(next 空) 
## 链表元素的添加
本质是对next 指针操作
前驱节点 

假设数组的长度是n, 因增加/删除操作导致需要移动的元素数量，
随着数组长度n的增大而增大， 呈一个线性关系 。
增加，删除操作对应的复杂度是O(n)

- js 数组的特性
