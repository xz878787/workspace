# 需要掌握的数据解构 


## 列表
- 数组
很多语言内置的数据结构 (开箱即用)

- 链表
- 栈
- 队列
- 树
    二叉树

    ## 怎么学习复习数据结构
    面向Javascript 
    面向面试
    hot 100 

    - 需要注意的问题
    不要基于做题， 要迁移语言
没有强调每一项类型一样
也不需要限制length 
- 内存地址
起始地址+偏移量

## 数组
    开箱即用
## 数组的创键

- ADT 的认识
  认识的存储空间 + 特定的操作
  
  - push ,pop,shift,unshift
  - 修改原数组，破坏了原来的数组  ，这些方法都不是纯函数
  - push 返回结果是新长度
  - 修改原数组
  - pop 移除的最后一个元素的值
  - unshift 在队头插入的元素， 返回结果是新长度
  - shift 在队头
- 纯函数
- new Array(7)
指定了长度的空数组
[empty × 7]
empty: 数组的这个位置还没有被占据
不属于任何类型
arr[0] undefined
- (new Array(7)).fill(1)
创建一个长度确定，同时每一个元素也
确定的值的数组

## 数组的访问和遍历
arr[0] //索引， 下标
## 遍历的数组
- for  计数循环
    机器化, 命令式,缺点是可读性不好
    优点：性能最好 
- for of
    语义很好
- for in
- forEach
   入 调用栈  执行上下文....
   优点是功能强大
   缺点是不能中途break;
- map、 filter 、every、some 
    基于forEach
   返回一个全新数组 纯函数
  filter 筛选 函数结果为true 留下
  every 每一项返回 true

怎么选择?

## 二维数组
矩阵  llm  向量矩阵

(new Array(7)).fill([])
这里fill的 []  是引用类型， fill 传的是入参的引用
本质
